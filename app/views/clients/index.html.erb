<div class="flex justify-between items-center mb-6">
  <h1 class="text-2xl font-bold text-gray-900">Mes clients</h1>
  <%= link_to "Nouveau client", new_client_path, class: "bg-indigo-600 text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-indigo-700 transition" %>
</div>

<% if @clients.any? %>
  <div class="bg-white shadow-sm rounded-xl border border-gray-200 overflow-hidden">
    <table class="min-w-full divide-y divide-gray-200">
      <thead class="bg-gray-50">
        <tr>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Nom</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Email</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Téléphone</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Type</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Actions</th>
        </tr>
      </thead>
      <tbody class="divide-y divide-gray-200">
        <% @clients.each do |client| %>
          <tr class="hover:bg-gray-50 transition">
            <td class="px-6 py-4 text-sm"><%= link_to client.name, client_path(client), class: "font-medium text-indigo-600 hover:text-indigo-500" %></td>
            <td class="px-6 py-4 text-sm text-gray-700"><%= client.email %></td>
            <td class="px-6 py-4 text-sm text-gray-700"><%= client.phone %></td>
            <td class="px-6 py-4 text-sm text-gray-700"><%= client.client_type == "professional" ? "Professionnel" : "Particulier" %></td>
            <td class="px-6 py-4">
              <div class="flex items-center space-x-2">
                <%= link_to client_path(client), title: "Voir", class: "text-gray-400 hover:text-indigo-600 transition" do %>
                  <%= icon("eye", css_class: "size-4") %>
                <% end %>
                <%= link_to edit_client_path(client), title: "Modifier", class: "text-gray-400 hover:text-indigo-600 transition" do %>
                  <%= icon("pencil-square", css_class: "size-4") %>
                <% end %>
                <%= button_to client_path(client), method: :delete, title: "Supprimer",
                    class: "text-gray-400 hover:text-red-600 transition", form: { data: { turbo_confirm: "Supprimer ce client ?" } } do %>
                  <%= icon("trash", css_class: "size-4") %>
                <% end %>
              </div>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
<% else %>
  <p class="text-gray-500 text-sm">Aucun client. <%= link_to "Créer votre premier client", new_client_path, class: "text-indigo-600 hover:text-indigo-500" %>.</p>
<% end %>